{
  "name": "ventuno.space",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "eleventy --serve & npx tailwindcss -i src/static/style.css -o dist/static/style.css --watch",
    "build": "rimraf dist && ELEVENTY_PRODUCTION=true eleventy && NODE_ENV=production tailwindcss -i src/static/style.css/ -o dist/static/style.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Alessandro Arnodo",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@tailwindcss/typography": "^0.5.2",
    "rimraf": "^3.0.2",
    "tailwindcss": "^3.0.24"
  }
}
